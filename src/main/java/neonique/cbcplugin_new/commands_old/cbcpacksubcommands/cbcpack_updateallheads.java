package neonique.cbcplugin_new.commands_old.cbcpacksubcommands;

import neonique.cbcplugin_new.commands_old._SubCommand;

public class cbcpack_updateallheads extends _SubCommand {

    /*public static void run(CBCPackCommand command, Player user, String[] args, int perms) {

        // Update player heads
        Integer successfullyUpdatedHeads = command.getResourcePackManager().updateAllPlayerHeads();

        if (successfullyUpdatedHeads == null) {
            user.sendMessage(Component.text("Error occured whilst updating heads! (Refer to logs)").color(NamedTextColor.YELLOW));
        }
        else {
            user.sendMessage(Component.text("Successfully updated " + successfullyUpdatedHeads + " heads!").color(NamedTextColor.GREEN));
        }
    }*/

}
